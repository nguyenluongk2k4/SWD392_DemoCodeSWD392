// Device Control - Domain - Actuator model alias
// Reuse centralized schema definition to maintain a single source of truth.

const { Actuator } = require('../../../database_schemas');

module.exports = Actuator;
